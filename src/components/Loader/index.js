import './index.css'

const Loader = () => (
  <div className="loader-container">
    <Loader
      className="loader"
      type="Oval"
      color="white"
      height={60}
      width={50}
    />
  </div>
)

export default Loader
